// Функція для обчислення суми двох чисел
function add(a, b) {
    return a + b;
}

// Функція для інвертування результату
function invertResult(result) {
    return -result;
}

module.exports = {
    add,
    invertResult,
};
